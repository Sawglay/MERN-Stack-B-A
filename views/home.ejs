<!DOCTYPE html>
<html lang="en">

<%- include ('./components/head.ejs') %>

    <body>

        <div class="container">
            <%- include ('./components/navbar.ejs') %>
                <%-"<h1>Hello World</h1>" %>
                    <% if(blogs.length) { %>
                        <% blogs.forEach(blog=> { %>
                           <div class="blog-card">
                             <h1>
                                <%= blog.title %>
                            </h1>
                            <p>
                                <%= blog.intro %>
                            </p>
                           </div>
                            <% }) %>
                                <% } %>
        </div>

    </body>

</html>